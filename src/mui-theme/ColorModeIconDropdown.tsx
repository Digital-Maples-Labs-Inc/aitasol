import * as React from 'react';
import { IconButtonOwnProps } from '@mui/material/IconButton';

export default function ColorModeIconDropdown(props: IconButtonOwnProps) {
  // Dark mode deactivated per user request
  return null;
}
